<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wannengyongyao.drug.dao.DrugUserWeixinMapper">
  <resultMap id="UserWeixin" type="com.wannengyongyao.drug.model.DrugWeixinUser">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="nick_name" jdbcType="VARCHAR" property="nickName" />
    <result column="unionid" jdbcType="VARCHAR" property="unionId" />
    <result column="openid" jdbcType="VARCHAR" property="openId" />
    <result column="gender" jdbcType="VARCHAR" property="gender" />
    <result column="avatar_url" jdbcType="VARCHAR" property="avatarUrl" />
    <result column="country" jdbcType="VARCHAR" property="country" />
    <result column="province" jdbcType="VARCHAR" property="province" />
    <result column="city" jdbcType="VARCHAR" property="city" />
  </resultMap>
  <sql id="Base_Column_List">
    id, nick_name, unionid, openid, gender, avatar_url, country, province, city
  </sql>
  <select id="get" parameterType="java.lang.Integer" resultMap="UserWeixin">
    select 
    <include refid="Base_Column_List" />
    from drug_user_wx
    where id = #{id,jdbcType=BIGINT}
  </select>

  <select id="getByOpenId" resultMap="UserWeixin">
    select
    <include refid="Base_Column_List" />
    from
      drug_user_wx
    where
      openid = #{openId}
  </select>

  <insert id="insert" parameterType="com.wannengyongyao.drug.model.DrugWeixinUser">
    insert into drug_user_wx (
      nick_name,
      unionid,
      openid,
      gender,
      avatar_url,
      country,
      province,
      city)
    values (
      #{nickName,jdbcType=VARCHAR},
      #{unionId,jdbcType=VARCHAR},
      #{openId,jdbcType=VARCHAR},
      #{gender,jdbcType=INTEGER},
      #{avatarUrl,jdbcType=VARCHAR},
      #{country,jdbcType=VARCHAR},
      #{province,jdbcType=VARCHAR},
      #{city,jdbcType=VARCHAR})
  </insert>
</mapper>