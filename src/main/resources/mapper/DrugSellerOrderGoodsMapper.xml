<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wannengyongyao.drug.dao.DrugSellerOrderGoodsMapper">
  <resultMap id="SellerOrderGoods" type="com.wannengyongyao.drug.model.DrugSellerOrderGoods">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="order_id" jdbcType="BIGINT" property="orderId" />
    <result column="drug_id" jdbcType="INTEGER" property="drugId" />
    <result column="order_drug_id" jdbcType="INTEGER" property="orderDrugId" />
    <result column="drug_name" jdbcType="VARCHAR" property="drugName" />
    <result column="unit_price" jdbcType="DECIMAL" property="unitPrice" />
    <result column="specifications" jdbcType="VARCHAR" property="specifications" />
    <result column="quantity" jdbcType="INTEGER" property="quantity" />
    <result column="manufacturer" jdbcType="VARCHAR" property="manufacturer" />
  </resultMap>
  <sql id="Base_Column_List">
    id, order_id, drug_id, order_drug_id, drug_name, unit_price, specifications, quantity, manufacturer
  </sql>

  <select id="get" parameterType="java.lang.Integer" resultMap="SellerOrderGoods">
    select 
    <include refid="Base_Column_List" />
    from
      drug_seller_order_goods
    where
      id = #{id,jdbcType=INTEGER}
  </select>

  <insert id="insert" parameterType="com.wannengyongyao.drug.model.DrugSellerOrderGoods">
    insert into drug_seller_order_goods (
      order_id,
      order_drug_id,
      seller_id,
      drug_id,
      drug_name,
      unit_price,
      specifications,
      quantity,
      manufacturer,
      photos,
      remark
    )
    values
    <foreach collection ="list" item="goods" separator =",">
    (
      #{goods.orderId,jdbcType=BIGINT},
      #{goods.orderDrugId,jdbcType=INTEGER},
      #{goods.sellerId,jdbcType=BIGINT},
      #{goods.drugId,jdbcType=INTEGER},
      #{goods.drugName,jdbcType=VARCHAR},
      #{goods.unitPrice,jdbcType=DECIMAL},
      #{goods.specifications,jdbcType=VARCHAR},
      #{goods.quantity,jdbcType=INTEGER},
      #{goods.manufacturer,jdbcType=VARCHAR},
      #{goods.photos,jdbcType=VARCHAR},
      #{goods.remark,jdbcType=VARCHAR}
    )
    </foreach>
  </insert>

  <select id="getByOrderAndSeller" resultMap="SellerOrderGoods">
    select
      <include refid="Base_Column_List" />
    from
      drug_seller_order_goods
    where
      order_id = #{orderId,jdbcType=BIGINT}
    and
      seller_id = #{sellerId,jdbcType=BIGINT}
  </select>

  <select id="getFirstDrugName" resultType="java.lang.String">
    SELECT
      drug_name
    FROM
      drug_seller_order_goods
    WHERE
      order_id = #{orderId}
    LIMIT 0,1
  </select>
</mapper>