<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wannengyongyao.drug.dao.DrugCouponMapper">
  <resultMap id="Coupon" type="com.wannengyongyao.drug.model.DrugCoupon">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="code" jdbcType="VARCHAR" property="code" />
    <result column="amount" jdbcType="INTEGER" property="amount" />
    <result column="start_date" jdbcType="DATE" property="startDate" />
    <result column="end_date" jdbcType="DATE" property="endDate" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="status" jdbcType="INTEGER" property="status" />
  </resultMap>

  <sql id="Base_Column_List">
    id, code, amount, start_date, end_date, status, create_time
  </sql>

  <select id="get" resultMap="Coupon">
    SELECT
      <include refid="Base_Column_List" />
    FROM
      drug_coupon
    WHERE
      code = #{code}
  </select>

  <insert id="insert" parameterType="com.wannengyongyao.drug.model.DrugCoupon">
    insert into drug_coupon (
      `code`,
      amount,
      start_date,
      end_date,
      status,
      create_time
    )
    values (
      #{code,jdbcType=VARCHAR},
      #{amount,jdbcType=INTEGER},
      #{startDate,jdbcType=DATE},
      #{endDate,jdbcType=DATE},
      #{status,jdbcType=INTEGER},
      #{createTime,jdbcType=TIMESTAMP})
  </insert>

  <select id="myCoupons" resultType="com.wannengyongyao.drug.model.DrugUserCoupon">
    SELECT
      c.code,
      c.amount,
      c.start_date as startDate,
      c.end_date as endDate,
      uc.status
    FROM
      drug_user_coupon uc
    INNER JOIN
      drug_coupon c
    ON
      uc.coupon_code = c.code
    WHERE
      uc.user_id = #{userId}
    ORDER BY uc.create_time DESC
  </select>

  <update id="changeUserCouponStatus">
    UPDATE
      drug_user_coupon
    SET
      status = #{status}
    WHERE
      coupon_code = #{code}
  </update>

  <select id="getUserCoupon" resultType="com.wannengyongyao.drug.model.DrugUserCoupon">
    SELECT
      c.code,
      c.amount,
      c.start_date as startDate,
      c.end_date as endDate,
      uc.status
    FROM
      drug_user_coupon uc
    INNER JOIN
      drug_coupon c
    ON
      uc.coupon_code = c.code
    WHERE
      uc.coupon_code = #{code}
  </select>

  <insert id="insertUserCoupon" parameterType="com.wannengyongyao.drug.model.DrugUserCoupon">
    INSERT INTO drug_user_coupon (
      user_id,
      coupon_code,
      create_time,
      status
    )
    VALUES(
      #{userId},
      #{code},
      #{createTime},
      #{status}
    )
  </insert>
</mapper>